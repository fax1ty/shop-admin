import React from "react";
import ReactDOM from "react-dom";
import "./index.scss";

ReactDOM.render(
  <React.StrictMode>
    <div className="app">Everything is working</div>
  </React.StrictMode>,
  document.getElementById("root")
);
